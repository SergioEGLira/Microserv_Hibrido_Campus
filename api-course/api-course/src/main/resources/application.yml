spring:
  application:
    name: api-course
  config:
    import: 'configserver:'
  cloud:
    config:
      discovery:
        serviceId: config-server
      username: configserver
      password: '123cs'
