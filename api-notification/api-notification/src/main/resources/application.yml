spring:
  application:
    name: api-notification
  config:
    import: 'configserver:'
  cloud:
    config:
      discovery:
        serviceId: config-server
      username: configserver
      password: '123cs'
